{
    "name":  "common-ts",
    "version":  "1.0.0",
    "private":  true,
    "description":  "Shared TypeScript packages for auth, game, cashier, profile, and UI stores",
    "workspaces":  [
                       "packages/*"
                   ],
    "scripts":  {
                    "build":  "npm run --workspace @common/shared-ui build \u0026\u0026 npm run --workspace @common/cashier build \u0026\u0026 npm run --workspace @common/profile build \u0026\u0026 npm run --workspace @common/auth build \u0026\u0026 npm run --workspace @common/game build",
                    "typecheck":  "npm run --workspaces --if-present typecheck",
                    "clean":  "npm run --workspaces --if-present clean"
                },
    "devDependencies":  {
                            "svelte":  "^5.0.0",
                            "typescript":  "^5.4.0"
                        },
    "dependencies":  {
                         "my-app":  "file:../betakwaaba"
                     }
}
